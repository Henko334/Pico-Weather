This is the code i used to build a small weather station using a pi pico and a BME280 sensor. It gets the readings then logs it to a database locally on my network.
It also acts as a webserver so i can go to the IP and see the current readings

The BME280 code file is thge code for the sensor. 
